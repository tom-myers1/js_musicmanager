// clears boxes

function clearBoxes(box) {
  //clear box
  let all = [all boxes];
  if (box === 'all') {
    // clear all boxes on active page if poss
    // if not clear all
    for (var i = 0; i < all.length; i++) {
      // i.reset value

    }
  } else {
    /// clear named boxes
    box.clear value 
  }
}
